---
id: 3
version: "9.2.0"
date: 2026-07-31
commit: "808489d99e78d7e981a51d9ff6c8b015fb04e466"
line: null
requirement_refs: [1, 2, 3, 4, 5]
---

## Highlights

Current release — matches `package.json`'s `version` at the point this
capsule was authored (2026-08-02). `publish/electron-builder.json` targets
in force at this release: `.dmg`/`.zip` (macOS), `.deb`/`.rpm` (Linux, per
`decisions/0002`), `nsis` (Windows) — the full spread requirement 0005
names.

## Notes

`line: null` — this project has no `lines/` records and no evidence of
parallel-maintained release branches; every tag in the history is on one
stream. Omitting `lines/` entirely (rather than inventing one) is what the
format calls for when there's nothing to axis against (SPEC §2.5).
