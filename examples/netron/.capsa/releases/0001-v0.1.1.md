---
id: 1
version: "0.1.1"
date: 2017-11-20
commit: "9a364cdb201c1966b6d5437a4b4df032dab0b3c6"
requirement_refs: [1]
---

## Highlights

The earliest tag in the repository's history. Predates `package.json`
(added six days later, 2017-11-16 — dates in a young repo don't always
sort the way a changelog reader expects) and predates the Electron desktop
build entirely: at this point Netron is the browser viewer alone.

## Notes

The commit history goes back to 2010 under a different identity before
the project became "Netron"; `CITATION.cff` dates the citable project to
2017-12-04. This release is the oldest point this capsule treats as
"Netron" rather than prehistory — see `insights/dev/pre-netron-history.md`.
